===========================
Pelican - category template
===========================

Load a template for an article based on its category. If a template named
``article-<category>.html`` exists in the theme, it will be used. If not, then
the default template of the theme is used.
